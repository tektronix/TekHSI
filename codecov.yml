---
coverage:
  status:
    project:
      default:
        target: 67%  # TODO: set to 100%
        threshold: 0%
    patch:
      default:
        target: 0%  # TODO: set to 100%
        threshold: 0%
