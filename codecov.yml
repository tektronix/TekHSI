---
coverage:
  status:
    project:
      default:
        target: 60%  # TODO: set to 100%
        threshold: 0%
    patch:
      default:
        target: 25%  # TODO: set to 100%
        threshold: 0%
